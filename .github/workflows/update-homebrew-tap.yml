name: Update Homebrew Tap

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to update (e.g., v1.0.0)'
        required: true

jobs:
  update-tap:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main repo
        uses: actions/checkout@v4

      - name: Get release info
        id: release
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
            TARBALL_URL="https://github.com/${{ github.repository }}/archive/${VERSION}.tar.gz"
          else
            VERSION="${{ github.event.inputs.version }}"
            TARBALL_URL="https://github.com/${{ github.repository }}/archive/${VERSION}.tar.gz"
          fi
          
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "tarball_url=${TARBALL_URL}" >> $GITHUB_OUTPUT
          
          # Download and calculate SHA256
          curl -L -o tamedia-tools.tar.gz "${TARBALL_URL}"
          SHA256=$(shasum -a 256 tamedia-tools.tar.gz | awk '{print $1}')
          echo "sha256=${SHA256}" >> $GITHUB_OUTPUT

      - name: Update Homebrew formulas and create PR
        env:
          GH_TOKEN: ${{ secrets.PULL_REQUEST_ACTION }}
        run: |
          VERSION="${{ steps.release.outputs.version }}"
          SHA256="${{ steps.release.outputs.sha256 }}"
          VERSION_NUM="${VERSION#v}"
          BRANCH_NAME="update-${VERSION}"

          # Clone the homebrew tap repo
          git clone https://github.com/dnd-it/homebrew-tamedia-tools.git homebrew-tap
          cd homebrew-tap

          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Create branch
          git checkout -b "$BRANCH_NAME"

          # Update each formula
          for formula in Formula/*.rb; do
            echo "Updating $(basename $formula)..."

            # Update version, URL, and SHA256
            sed -i "s|url \".*\"|url \"https://github.com/${{ github.repository }}/archive/${VERSION}.tar.gz\"|g" "$formula"
            sed -i "s|sha256 \".*\"|sha256 \"${SHA256}\"|g" "$formula"
            sed -i "s|version \".*\"|version \"${VERSION_NUM}\"|g" "$formula"
            sed -i "s|PLACEHOLDER_SHA256|${SHA256}|g" "$formula"
          done

          # Commit changes
          git add -A
          git commit -m "Update formulas for ${VERSION}"

          # Push branch
          git push origin "$BRANCH_NAME"

          # Create PR using gh CLI
          gh pr create \
            --title "Update to ${VERSION}" \
            --body "Updates Homebrew formulas to version ${VERSION}

          - Updated URL to point to ${VERSION} release
          - Updated SHA256: \`${SHA256}\`
          - Updated version to \`${VERSION_NUM}\`

          This PR was automatically generated by the release workflow.

          **Testing:**
          \`\`\`bash
          brew tap dnd-it/tamedia-tools
          brew install tamedia-tools
          tamedia-tunnel --version
          tamedia-aws-cli-helpers --version
          \`\`\`" \
            --head "$BRANCH_NAME" \
            --base main \
            --repo dnd-it/homebrew-tamedia-tools