#compdef tamedia-tunnel

_tamedia_tunnel() {
    local -a options
    options=(
        '(-h --help)'{-h,--help}'[Show help message]'
        '(-v --version)'{-v,--version}'[Show version information]'
    )
    
    _arguments -s $options
}

_tamedia_tunnel "$@"